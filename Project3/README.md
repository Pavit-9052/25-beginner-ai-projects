## 📝 AI-Powered Grammar and Spell Checker

A simple AI-powered grammar and spelling correction tool built with Gradio.
It uses Ollama (DeepSeek model) to correct user-provided text, providing a clean, interactive interface for improving grammar and spelling in multiple lines of text.

## 🚀 Features

Corrects grammar and spelling errors in sentences or paragraphs.
Returns only the corrected text without explanations or “thinking” steps.
Handles multi-line input.
Clean and interactive web interface built with Gradio.
Powered by Ollama API running the DeepSeek model.

## 📂 How It Works

1. Enter the text containing grammar and spelling errors in the input box.
2. Click Submit.
3. The AI model processes the text and returns the corrected version in the output box.
4. The output box supports multi-line display and scrolls automatically if the text exceeds the box size.


## 🖥️ Tech Stack

* **Python**
* **Gradio** – for the user interface
* **Requests** – for handling API calls
* **Ollama API** – DeepSeek model for text generation

📷 Example Output

![Output Screenshot](screenshot.png)
